# Uncomment the next line to define a global platform for your project
platform :ios, '16.0'

target 'ShoeStore' do
  use_frameworks!
  
  pod 'Core', :git => 'https://github.com/felipeassis97/Core.git', :tag => '1.3.0'

end
